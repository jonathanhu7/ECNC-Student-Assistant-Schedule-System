import React from "react";
import { Box } from "@mui/material";

export interface LayoutPropos {
  children: React.ReactNode;
}

// TODO: 完成 auth 的布局
export function Layout({ children }: LayoutPropos): React.ReactElement {
  return <Box></Box>;
}
